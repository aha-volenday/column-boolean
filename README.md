# @volenday/column-boolean

> React Table column for boolean.

## Install

Using npm:

```sh
npm install --save @volenday/column-boolean
```

or using yarn:

```sh
yarn add @volenday/column-boolean
```
